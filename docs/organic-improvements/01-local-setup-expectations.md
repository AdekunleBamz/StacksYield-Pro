# Local Setup Expectations

- Use Node 18 before running workspace scripts.
- Run the setup script from repo root for deterministic install.
- Confirm frontend environment variables are populated.
- Keep one funded test wallet for strategy smoke tests.
